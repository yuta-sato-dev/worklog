[package]
name = "fetch-focused-window"
version = "0.3.0"
edition = "2024"

[dependencies]
active-win-pos-rs = "0.11"
chrono = { version = "0.4", features = ["serde"] }
rusqlite = { version = "0.32", features = ["bundled"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
